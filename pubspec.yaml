name: shopping_cart_app
description: A new Flutter project.

# Prevent accidental publication to pub.dev
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.7.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^7.3.3
  cupertino_icons: ^1.0.8
  http: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Include assets for the project
  assets:
    - assets/
